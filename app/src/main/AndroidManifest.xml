<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher_dog"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_dog_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Animals"
        tools:targetApi="31">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Animals">
        </activity>

        <activity-alias
            android:name=".MainActivityDog"
            android:targetActivity=".MainActivity"
            android:enabled="true"
            android:label="@string/app_name"
            android:exported="true"
            android:icon="@mipmap/ic_launcher_dog"
            android:roundIcon="@mipmap/ic_launcher_dog">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>

        <activity-alias
            android:name=".MainActivityCat"
            android:targetActivity=".MainActivity"
            android:enabled="false"
            android:label="@string/app_name"
            android:exported="true"
            android:icon="@mipmap/ic_launcher_cat"
            android:roundIcon="@mipmap/ic_launcher_cat_round">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>

        <activity-alias
            android:name=".MainActivityParrot"
            android:targetActivity=".MainActivity"
            android:enabled="false"
            android:label="@string/app_name"
            android:exported="true"
            android:icon="@mipmap/ic_launcher_parrot"
            android:roundIcon="@mipmap/ic_launcher_parrot_round">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>

    </application>

</manifest>